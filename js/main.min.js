"use strict";

const button = document.querySelector(".main-nav__button"),
  modal = document.querySelector(".modal"),
  modalFirstLink = modal.querySelector(".contacts-list__map-link"),
  changeModalVisability = () => {
    modal.classList.contains("modal-show") ? modal.classList.remove("modal-show") : (modal.classList.add("modal-show"), modalFirstLink.focus());
  };
modal.addEventListener("click", a => {
  changeModalVisability();
}), modal.addEventListener("keydown", a => {
  "Escape" === a.key && changeModalVisability();
}), button.addEventListener("click", a => {
  changeModalVisability();
}), button.addEventListener("keydown", a => {
  "Enter" === a.key && changeModalVisability();
});
//# sourceMappingURL=main.min.js.map
